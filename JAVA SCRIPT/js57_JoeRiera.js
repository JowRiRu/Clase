
// El problema de este es que no tiene var delante.
Edad = 50

// El problema de esta variable es que no tiene un igual indicando cual es el valor de la variable.
var b, c;

// La variable esta esta mas ya que para empezar no puedes indicar la variable de esta manera, tampoco tiene = al igual que la anterior.
var d, var e;

// En la siguiente variable lo que le falta es el nombre de la variable o el valor que querias darle a la variable dependiendo de lo que quisieramos hacer con la variable.
var default;

// La siguiente variable el rpoblema que tiene es que para empezar no indicamos que es una variable tampcoo el = ni nada para identificar el valor de la variable.
_mes

// En la siguiente variable lo que pasa es que otra vez como en la anterior no indica que es una variable y al igual que la otra no especifica ningun valor ni nombre.
$_mes;

// En esta variable lo que falla es que no especificas que es una variable ni el valor de la misma, tampoco puede empezar asi el nombre de la variable. Es decir que no puedes poner el nombre de una variable con un numero delante.
01_enero

//La siguiente variable lo que falla es que no especifica el valor de la variable. aparte de que no se puede repetir de variable en la misma frase.
var primer apellido; var segundo.apellido;

// En la siguiente variable especifica valor lo que lo especifica mal ya que tendria que poner el valor de la variable entre parentesis y comillas delante y al final de la palabra.
var despierto = dormido = 0;

// En la siguiente var lo 'unico que le faltaba es rodear al palor "perro" entre parentesis.
var animal = "perro";

// En la siguiente var al igual que la anterior lo que le falta es que no se cierra bien el valor de la variable. La solucion es rodear a seat por un parentesis y ponerle entre comillas.
var coche = seat; 